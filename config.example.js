/**
 * Скопируй в config.js и подставь ключ:
 *   cp config.example.js config.js
 *
 * config.js добавь в .gitignore, чтобы не попал в публичный GitHub.
 */
window.FUNDUK_CONFIG = {
  GEMINI_API_KEY: ''  // ← ключ из Google AI Studio
};
